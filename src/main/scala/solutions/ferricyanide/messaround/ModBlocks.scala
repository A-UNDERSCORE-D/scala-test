package solutions.ferricyanide.messaround

import solutions.ferricyanide.messaround.blocks.{BlockBase, BlockTest}

object ModBlocks {
  var blockList: List[BlockBase] = List(BlockTest)
  private val blockTest = BlockTest
}
